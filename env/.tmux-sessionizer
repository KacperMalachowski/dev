#!/usr/bin/env bash

if [[ "$(pwd)" == $HOME/personal ]]; then
    clear
    return
fi

if [[ "$(pwd)" == $HOME/work ]]; then
    clear
    return
fi

tmux new-window -dn scratch
tmux new-window -dn claude "bash -c 'claude 2>>/tmp/claude-psa.log'"
vim .
clear
