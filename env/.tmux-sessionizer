#!/usr/bin/env bash

if [[ "$(pwd)" == $HOME/personal ]]; then
    clear
    return
fi

if [[ "$(pwd)" == $HOME/work ]]; then
    clear
    return
fi

tmux new-window -dn scratch
tmux new-window -dn opencode "opencode . --print-logs 2>>/tmp/opencode-psa.log"
vim .
clear
