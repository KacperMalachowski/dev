#!/usr/bin/env bash

# Install OpenCode CLI tool
if ! command -v opencode &> /dev/null; then
    curl -sSL https://opencode.ai/install | bash -s -- --no-modify-path
else
    echo "OpenCode is already installed."
fi

